# QuillTTY

QuillTTY makes it easier to build terminal applications in Rust. It provides a simple, ergonomic API for rendering styled text, updating the screen efficiently, and working with text boxes and widgets. By building on top of `crossterm`, QuillTTY also offers straightforward handling for mouse and keyboard input, raw mode, and other cross-platform terminal features.
